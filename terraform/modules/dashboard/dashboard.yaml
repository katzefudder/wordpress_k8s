extraArgs:
  - --enable-skip-login
  - --enable-insecure-login
  - --system-banner="Welcome to Kubernetes"

#protocolHttp: true
ingress:
  enabled: true
  className: "nginx"
  path: /
  host: dashboard.localhost