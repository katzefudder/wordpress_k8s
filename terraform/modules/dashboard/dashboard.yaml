extraArgs:
#  - --enable-skip-login
  - --enable-insecure-login
#  - --system-banner="Welcome to Kubernetes"

service:
  externalPort: 8080
protocolHttp: true
ingress:
  enabled: true
  className: "nginx"
  path: /
  host: dashboard.localhost